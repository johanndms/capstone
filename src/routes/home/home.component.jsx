import Directory from '../../components/directory/directory.component';

/**
 * Main "Home" ROUTE that will call the Directory function with all the categories.
 * The Directory function will build the JSX for the Home Page
 *
 * @returns
 */
const Home = () => {
   return <Directory />;
};

export default Home;
